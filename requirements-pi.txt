# Requirements for Pi Zero deployment
# Uses relative paths for local jarvis packages

paho-mqtt
httpx

# openwakeword is installed separately with --no-deps in build-tarball.sh
# to avoid pulling ai-edge-litert (no armv7l wheel).
# These are the openwakeword deps we actually need (onnx inference):
# onnxruntime: PyPI has arm64 wheels; armv7l has no cp313 wheel so it is
# installed best-effort in build-tarball.sh (wake word detection is
# unavailable on armv7l if this fails).
onnxruntime>=1.24.3,!=1.25.1
scikit-learn>=1,<2
# speexdsp-ns: no armv7l wheel or source dist available, skipped
# noise suppression is optional for openwakeword

pyaudio
vosk
sounddevice
numpy<2
scipy
requests

# ReSpeaker 2-mics Pi HAT v2 — RGB LEDs (SPI) + user button (GPIO17).
# spidev has armv7l wheels; gpiozero + apa102-pi are pure Python.
# Pin backend (lgpio) is provided by the Pi-OS-Bookworm/Trixie system
# package `python3-lgpio` (apt-installed by install.sh) and visible to
# the venv because we create it with --system-site-packages. The lgpio
# PyPI package needs liblgpio-dev to compile, which isn't in plain
# Debian (only Pi OS), so the build container can't produce a wheel.
spidev
apa102-pi
gpiozero
aiodiscover
websockets
aiohttp
tqdm
zeroconf
manuf
cryptography>=41.0
sqlcipher3
sqlalchemy
alembic
pytz
pydantic
python-dotenv
pyyaml
fastapi>=0.100.0
uvicorn>=0.22.0
packaging>=21.0

# Local jarvis packages (relative to /home/pi/projects/)
jarvis-log-client @ git+https://github.com/alexberardi/jarvis-log-client.git@c2f8e82
jarvis-config-client @ git+https://github.com/alexberardi/jarvis-config-client.git@fea99b556e2ac09a67101bd5e18af1dd4c923e4f
