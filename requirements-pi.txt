# Requirements for Pi Zero deployment
# Uses relative paths for local jarvis packages

paho-mqtt
httpx

# openwakeword is installed separately with --no-deps in shared.sh
# to avoid pulling ai-edge-litert (no armv7l wheel).
# These are the openwakeword deps we actually need (onnx inference):
# onnxruntime: no PyPI wheel for armv7l, use community-built wheel
onnxruntime @ https://raw.githubusercontent.com/nknytk/built-onnxruntime-for-raspberrypi-linux/master/wheels/bullseye/onnxruntime-1.16.0-cp311-cp311-linux_armv7l.whl
scikit-learn>=1,<2
# speexdsp-ns: no armv7l wheel or source dist available, skipped
# noise suppression is optional for openwakeword

pyaudio
vosk
sounddevice
numpy<2
scipy
requests
aiodiscover
websockets
aiohttp
tqdm
zeroconf
manuf
cryptography>=41.0
pysqlcipher3
sqlalchemy
alembic
pytz
pydantic
python-dotenv
fastapi>=0.100.0
uvicorn>=0.22.0
packaging>=21.0

# Local jarvis packages (relative to /home/pi/projects/)
jarvis-log-client @ git+https://github.com/alexberardi/jarvis-log-client.git
jarvis-config-client @ git+https://github.com/alexberardi/jarvis-config-client.git
