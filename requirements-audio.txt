# Audio + wake-word dependencies. Install on top of requirements-base.txt.
# Cross-platform: works on a native Pi AND in the container / generic Linux.
# HAT-only libs (spidev/apa102-pi/gpiozero) live in requirements-hat.txt —
# they can't install off-Pi.
pyaudio
sounddevice
vosk
# openwakeword installed separately with --no-deps (avoids ai-edge-litert,
# which has no wheel on these arches)
onnxruntime>=1.10.0,<2,!=1.25.1
scikit-learn>=1,<2
scipy
numpy
