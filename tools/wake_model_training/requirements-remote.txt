# Dependencies installed ON the RunPod pod (torch ships with the image:
# runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04).
#
# openWakeWord itself is installed editable from the PINNED clone in
# remote_train_wake.py stage_setup (pip install -e /workspace/openwakeword),
# and piper-sample-generator's own requirements.txt is installed there too —
# this file covers the training extras the automatic_model_training.ipynb
# notebook installs on top.

pyyaml
numpy
scipy<1.17  # acoustics needs scipy.special.sph_harm, removed in 1.17
huggingface_hub

# openWakeWord training extras (per notebooks/automatic_model_training.ipynb
# at the pinned ref):
mutagen
torchinfo
torchmetrics
speechbrain
audiomentations
torch-audiomentations
acoustics
pronouncing
datasets
deep-phonemizer

# .tflite export path in train.py
tensorflow-cpu
onnx<1.17  # onnx_tf needs onnx.mapping, removed in 1.17
onnx_tf
piper-tts  # generate_samples.py imports piper.PiperVoice (needed even at the ded9350 pin)
tensorflow-probability  # onnx_tf transitive dep, not declared by the abandoned package
tf-keras  # tensorflow-probability needs standalone keras 2
