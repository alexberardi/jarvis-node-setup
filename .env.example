# Example .env for Jarvis Node secret storage

# Directory where secrets and keys are stored (default: ~/.jarvis)
JARVIS_SECRET_DIRECTORY=$HOME/.jarvis

# Path to the Fernet encryption key file (default: $JARVIS_SECRET_DIRECTORY/secrets.key)
JARVIS_KEY_FILE=$JARVIS_SECRET_DIRECTORY/secrets.key

# Path to the encrypted secrets JSON file (default: $JARVIS_SECRET_DIRECTORY/secrets.json.enc)
JARVIS_SECRET_FILE=$JARVIS_SECRET_DIRECTORY/secrets.json.enc