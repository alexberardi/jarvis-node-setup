# Minimal requirements for provisioning mode only
# For full jarvis-node functionality, use requirements-pi.txt
#
# This is a lightweight dependency set for headless Pi Zero nodes
# that only need to run the provisioning server (WiFi setup via mobile app).
# Installs much faster than the full requirements-pi.txt.

# Web framework for provisioning API
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic
python-dotenv

# Encrypted credential storage
cryptography>=41.0

# Local jarvis packages (relative to /home/pi/projects/)
jarvis-log-client @ git+https://github.com/alexberardi/jarvis-log-client.git
