paho-mqtt
httpx
# openwakeword is installed separately with --no-deps in shared.sh
# to avoid pulling ai-edge-litert (no aarch64 wheel).
# These are the openwakeword deps we actually need (onnx inference):
onnxruntime>=1.10.0,<2
scikit-learn>=1,<2
pyaudio
vosk
sounddevice
numpy
scipy
requests
aiodiscover
pytest
pytest-asyncio
websockets 
aiohttp
tqdm
zeroconf
manuf
cryptography>=41.0
pysqlcipher3
sqlalchemy
alembic
pytz
pydantic
dotenv
fastapi>=0.100.0
uvicorn>=0.22.0
jarvis-log-client @ git+https://github.com/alexberardi/jarvis-log-client.git
