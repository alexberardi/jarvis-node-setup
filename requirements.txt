paho-mqtt
httpx
# openwakeword is installed separately with --no-deps in shared.sh
# to avoid pulling ai-edge-litert (no aarch64 wheel).
# These are the openwakeword deps we actually need (onnx inference):
onnxruntime>=1.24.3,!=1.25.1,<2
scikit-learn>=1.7.2,<2
pyaudio
vosk
sounddevice
numpy
scipy
requests
aiodiscover
pytest
pytest-asyncio
websockets
aiohttp
tqdm
zeroconf
pyatv
manuf
cryptography>=50.0.0
# 0.6.2 is the first release with Windows wheels (win32/win_amd64/win_arm64)
# and the first the aarch64 Pi installs as a wheel rather than a source build.
sqlcipher3>=0.6.2,<0.7.0
sqlalchemy
alembic
pytz
pydantic
dotenv
fastapi>=0.141.1
uvicorn>=0.52.4
packaging>=26.3
jarvis-log-client @ git+https://github.com/alexberardi/jarvis-log-client.git@c2f8e82
