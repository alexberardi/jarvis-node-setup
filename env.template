# =============================================================================
# jarvis-node-setup - Environment Template
# =============================================================================
# Copy to .env and fill in values
# Client software for Pi Zero voice nodes
# =============================================================================

# Service URLs are resolved automatically from jarvis-config-service

# -----------------------------------------------------------------------------
# NODE IDENTITY
# -----------------------------------------------------------------------------
# Node operating system: LINUX, MACOS, or RASPBERRY_PI
JARVIS_NODE_OS=LINUX

# Path to config.json (node-specific settings)
CONFIG_PATH=/path/to/jarvis-node-setup/config.json

# -----------------------------------------------------------------------------
# COMMAND CENTER
# -----------------------------------------------------------------------------
# Node API key (get from jarvis-command-center admin)
NODE_API_KEY=CHANGE_ME_get_from_command_center

# -----------------------------------------------------------------------------
# SECRET STORAGE
# -----------------------------------------------------------------------------
# Directory for encrypted secrets
JARVIS_SECRET_DIRECTORY=$HOME/.jarvis

# Key file location
JARVIS_KEY_FILE=$JARVIS_SECRET_DIRECTORY/secrets.key

# Encrypted secrets file
JARVIS_SECRET_FILE=$JARVIS_SECRET_DIRECTORY/secrets.json.enc

# Master key for encryption (generate with: openssl rand -hex 32)
JARVIS_MASTER_KEY=CHANGE_ME_generate_secure_key

# SQLite database for node data (used by PySQLCipher)
JARVIS_NODE_DB=./jarvis_node.db

# -----------------------------------------------------------------------------
# WAKE WORD (Porcupine)
# -----------------------------------------------------------------------------
# Picovoice access key (get from: https://console.picovoice.ai/)
PORCUPINE_ACCESS_KEY=CHANGE_ME_picovoice_key

# Wake word to listen for (default: jarvis)
WAKE_WORD=jarvis

# -----------------------------------------------------------------------------
# MQTT (for TTS playback)
# -----------------------------------------------------------------------------
MQTT_BROKER=localhost
MQTT_PORT=1883
MQTT_TOPIC=jarvis/tts

# -----------------------------------------------------------------------------
# PROVISIONING
# -----------------------------------------------------------------------------
# Set to true to run provisioning in simulation mode (no real WiFi)
JARVIS_SIMULATE_PROVISIONING=false

# Port for provisioning API (default: 8080)
JARVIS_PROVISIONING_PORT=8080

# Set to true to skip provisioning check on startup (for development)
JARVIS_SKIP_PROVISIONING_CHECK=false
